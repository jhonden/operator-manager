# Operator Manager - 启动指南

## 🚀 快速启动步骤

### 前置检查

首先确认你已安装：
```bash
# 检查 Java（需要 21+）
java -version

# 检查 Maven
mvn -version

# 检查 Node.js
node -v

# 检查 npm
npm -v
```

## 方案一：使用数据库服务（推荐）

### 步骤 1: 启动数据库服务

```bash
# 在项目根目录执行
cd /Users/gaowen/Code/operator-manager

# 启动 PostgreSQL、Redis、MinIO
docker compose up -d
# 或者
docker-compose up -d
```

如果Docker未安装，可以手动安装这些服务，或者使用方案二。

### 步骤 2: 启动后端

```bash
# 打开一个新终端
cd /Users/gaowen/Code/operator-manager/operator-api

# 启动后端服务
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

等待看到类似这样的日志表示启动成功：
```
Started OperatorManagerApplication in XX seconds
```

### 步骤 3: 启动前端

```bash
# 打开另一个新终端
cd /Users/gaowen/Code/operator-manager/operator-manager-web

# 安装依赖（首次运行）
npm install

# 启动前端开发服务器
npm run dev
```

## 方案二：不使用Docker（简化版）

如果你不想安装Docker，可以修改配置使用H2内存数据库：

### 1. 修改配置文件

编辑 `operator-api/src/main/resources/application-dev.yml`，将数据库改为H2：

```yaml
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
```

### 2. 然后启动后端和前端（同方案一的步骤2和3）

## 📍 访问地址

启动成功后访问：

- 📱 **前端界面**: http://localhost:5173
- 📊 **后端API**: http://localhost:8080
- 📚 **API文档**: http://localhost:8080/swagger-ui.html
- 🗄️ **H2控制台**（如果使用H2）: http://localhost:8080/h2-console

## 🔑 首次使用

1. 访问 http://localhost:5173
2. 点击 "Register now" 注册账号
3. 填写信息完成注册
4. 登录系统开始使用

## ⚠️ 常见问题

### Maven 构建失败
```bash
# 清理后重新构建
cd operator-api
mvn clean install -U
```

### 前端依赖安装失败
```bash
cd operator-manager-web
rm -rf node_modules package-lock.json
npm install
```

### 端口已被占用
```bash
# 查看占用端口的进程
lsof -i :8080  # 后端
lsof -i :5173  # 前端

# 杀死进程
kill -9 <PID>
```

## 📝 启动脚本

### macOS/Linux

```bash
# 启动后端（在 operator-api 目录）
mvn spring-boot:run -Dspring-boot.run.profiles=dev

# 启动前端（在 operator-manager-web 目录）
npm run dev
```

### Windows

```cmd
REM 启动后端
cd operator-api
mvn spring-boot:run -Dspring-boot.run.profiles=dev

REM 启动前端（新终端）
cd operator-manager-web
npm run dev
```

## 🎯 开发模式

- **后端**: 修改代码后自动重启（Spring Boot DevTools）
- **前端**: 修改代码后浏览器自动刷新（Vite HMR）

## 🛑 停止服务

在各自终端按 `Ctrl + C` 停止服务。

如果使用了Docker：
```bash
docker compose down
```

---

**开始使用 Operator Manager！** 🚀
